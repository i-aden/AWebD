<script setup>
// Import the RandomMessage component here.

</script>

<template>
  <div>
    <!--
      1. Now that the 'RandomMessage component has been imported', render it in this template.
      2. Set values for the `editor`, 'language' and `price` prop attribute values on the component:
        a. first using the <CamelCase /> style and then
        b. using the <kebab-case></kebab-case> style.
    -->
    ???
  </div>
</template>

<style>
/* Write styling here */
</style>


<!--
In this exercise, we have learned how props and prop passing can be used to increase reuse scenarios
of a component by generalizing it. 

Instead of the component rendering static data, its parent passes it the data to render.
-->