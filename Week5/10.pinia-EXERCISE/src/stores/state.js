import { defineStore } from 'pinia'

export const useStateStore = defineStore('counter', () => {
  const state = {
    name:"Lindy",
    favoriteColor: "blue",
    profession: "Librarian"
  }

  return { ? }
})
