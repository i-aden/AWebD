// FIX
// EXERCISE: Displaying State Values
import { defineStore } from 'pinia'

export const useStateStore = Store('counter', () => {
  const state = {
    name:"Lindy",
    favoriteColor: "blue",
    profession: "Librarian"
  }

  return { ? }
})
